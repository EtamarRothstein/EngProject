1. Add the Dataset to your directory
2. Run DataPreparation to create train, val and experiment sub-datasets
3. Run DecisionTree to create the decision tree model


Python 3.8

pip install adversarial-robustness-toolbox
pip install packaging
pip install transformers
pip install torch
pip install tqdm
pip install transformers[torch]
pip install git+https://github.com/facebookresearch/attack-tabular.git

